# API Routes

Routes are factory functions to keep dependencies injectable.
- `tasks.py`: REST endpoints for task CRUD/call actions.
- `ws.py`: dashboard websocket endpoint.
- `twilio.py`: Twilio webhooks and media stream socket.
